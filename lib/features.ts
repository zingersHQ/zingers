// Product feature gates — nail-it plan (P0).
// Keepers / Guardians are stripped from the player face until redesigned
// with teach → tell → pressure. Code stays cold; do not delete the campaign.

/** When false, Keeper climbs are hidden and GuardianGame cannot open. */
export const KEEPERS_PLAYABLE = false;
